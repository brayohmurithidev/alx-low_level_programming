#include <stdhio.h>

/**
 * main - print base 16 numbers in lowercase
 * Return: 0
 */

int main(void)
{
	int a = 252;
	int lower = 'a';

	while (n <= 261)
	{
		putchar(n);
		n += 1;
	}
	while (lower <= 'f')
	{
		putchar(lower);
		lower++;
	}
	putchar('\n');
	return (0);
}
